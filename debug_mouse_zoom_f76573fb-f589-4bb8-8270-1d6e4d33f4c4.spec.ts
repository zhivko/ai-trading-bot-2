
import { test } from '@playwright/test';
import { expect } from '@playwright/test';

test('debug_mouse_zoom_2025-10-05', async ({ page, context }) => {
  
    // Navigate to URL
    await page.goto('http://localhost:5000/BTCUSDT');
});